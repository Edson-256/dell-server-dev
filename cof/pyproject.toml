[project]
name = "cof"
version = "0.1.0"
description = "Agente de download automatizado para material de estudo"
requires-python = ">=3.11"
dependencies = [
    "playwright",
    "httpx",
    "python-dotenv",
    "schedule",
]

[project.scripts]
cof = "src.main:cli"
